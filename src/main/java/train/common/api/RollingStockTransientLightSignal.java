package train.common.api;

/** Non-persistent synchronized signals that may temporarily alter fixture behavior. */
public enum RollingStockTransientLightSignal
{
    /** Authenticated horn action currently lies within its synchronized response window. */
    HORN
}
